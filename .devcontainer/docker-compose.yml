version: '3.8'

services:
  app:
    image: node:20-bullseye
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    command: sleep infinity
    ports:
      - "3000:3000"
      - "5173:5173"
